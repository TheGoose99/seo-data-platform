# Cal.com onboarding (stub)

Reserved for a future phase: calendar embed, webhooks, or API provisioning after the website onboarding step.

`onboard-client.mjs` loads `index.mjs` when present and calls `runCalcomOnboarding()` if exported. The default implementation is a no-op.
